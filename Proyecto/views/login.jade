extends ./layout.jade
block contenido
	h1 Login Proyecto Index
	form(action="/users", method="POST")
		div
			label(for="email") Email
			input(type="text", name="email", placeholder="Email", id="email" class="form-control")
		div
			label(for="pass") Pass
			input(type="password", name="pass", placeholder="Pass", id="pass" class="form-control")
		div
			input(type="submit" value="Iniciar sesión" class="form-control" class="btn btn-info")